import { ReactElement } from "react";

const CourseCard = ({ course }): ReactElement => {
  console.log(course);
  return (
    <div>
      <h1>Course Card</h1>
    </div>
  );
};

export default CourseCard;
